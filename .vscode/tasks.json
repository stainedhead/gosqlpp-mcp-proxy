{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build proxy",
            "type": "shell",
            "command": "go build -o mcp_sqlpp_proxy main.go",
            "group": "build",
            "problemMatcher": ["$go"]
        }
    ]
}
